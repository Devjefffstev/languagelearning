package main

import "fmt"

func main() {
	var shortText = "Hello, defining a variable using var \n"
	var longText string = "Hello, defining a variable using var and string \n"
	text := "Hello, defining a variable using := \n"

	fmt.Print(shortText)
	fmt.Print(longText)
	fmt.Print(text)

}
