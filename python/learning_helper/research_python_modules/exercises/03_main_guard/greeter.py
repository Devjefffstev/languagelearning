# greeter.py — starter for Exercise 03.
# Fill in the function body and the __main__ guard so the module
# works both as an importable library and as a runnable script.

import sys


def greet(name):
    """Return a friendly greeting."""
    # TODO: build and return the greeting string using `name`.
    pass


if __name__ == "__main__":
    # TODO: read a name from sys.argv[1] (default to "World" if missing),
    # call greet(name), and print the result.
    pass