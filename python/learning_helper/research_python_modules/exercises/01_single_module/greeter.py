# greeter.py — starter for Exercise 01.
# Fill in the body of greet(name) so it returns "Hello, {name}!".

def greet(name):
    """Return a friendly greeting."""
    # TODO: build and return the greeting string using `name`.
    return "Hello, now you are my friend. nice to meet you " + name


def _secret():
    """Leading underscore signals 'internal use only'."""
    return "ssssh"