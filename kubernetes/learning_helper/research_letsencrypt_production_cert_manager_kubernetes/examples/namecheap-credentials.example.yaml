# Template only. Create this Secret in the same namespace as the
# Certificate that uses the Namecheap webhook. Do not commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: namecheap-credentials
  namespace: tls-demo
type: Opaque
stringData:
  apiKey: replace-with-namecheap-api-key
  apiUser: replace-with-namecheap-api-user
